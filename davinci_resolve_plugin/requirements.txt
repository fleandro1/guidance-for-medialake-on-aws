# DaVinci Resolve Media Lake Plugin Dependencies
# Core Qt Framework
PySide6>=6.5.0
# Note: PySide6 QtMultimedia is optional for video preview.
# If not available, thumbnails will be shown instead.
# On some systems, QtMultimedia may need to be installed separately
# or may require system-level multimedia libraries.

# AWS Authentication
boto3>=1.28.0
botocore>=1.31.0

# Secure Credential Storage
keyring>=24.0.0

# HTTP requests (for non-Qt contexts)
requests>=2.31.0

# Image processing for thumbnails
Pillow>=10.0.0

# macOS native integration (for file thumbnails on macOS)
pyobjc-framework-Cocoa>=9.0; sys_platform == 'darwin'

# Development dependencies
pytest>=7.4.0
pytest-qt>=4.2.0
pytest-asyncio>=0.21.0
